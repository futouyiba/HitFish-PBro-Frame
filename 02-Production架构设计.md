# 02 · Production 架构设计

> 前置：[01 对话恢复与推导档案](01-对话恢复与推导档案.md)。本文在「固定鱼习性模型」基础上向 Production 级下钻。
> 推导独立进行，不引用 Notion 其他设计分支。结论变更遵循原则：**具体化后暴露的隐藏复杂度，直接修正或推翻，并给出理由。**
> 更新记录 R1.1（2026-09-10）：[06 独立复核](06-独立复核与增补.md) 的 F1（份额幂归一）/ F2（AFIT veto 包络）/ F3（AnglerMatch 几何合成）已并入本文公式；F4 已决策为**事实侧平滑、内核无状态**。

---

## 一、生产级目标与非目标

### 目标
1. 把「固定鱼习性模型」具体化为可开发的数据契约、计算内核、烘焙管线与运行时接口；
2. 保持策划心智模型（填档案，不编程）与研发成本优势（固定计算器）；
3. 所有输出可解释（P/A/份额/原因）、可回放（版本+种子）、可灰度（按鱼种切换新旧）；
4. 足以支撑数百鱼种 × 多场景 × 35 切片/周的批量生产与验证。

### 非目标（边界，继承自原对话）
- 不改圆桌抽鱼、NONE、Generate Fish、Fish AI（只提供契约）；
- 保底/运营修正等 ControlModifier 完全外置，只保留输入口；
- 不做文本 DSL、节点编辑器（L1 条件行为上限，见 D 决策）；
- 不模拟 Fish AI 行为细节。

---

## 二、总架构

```text
┌─ 输入层 ──────────────────────────────────────────────┐
│ 关卡空间数据(SpatialTarget+Capacity)                    │
│ 环境管线(逐切片事实+派生事实+平滑)                        │
│ 食物/事件事实(饵鱼群/羽化/钓压，由外部状态系统提供)          │
└──────────────┬────────────────────────────────────────┘
               ↓
┌─ 配置层（策划编辑器，authoring）──────────────────────────┐
│ 空间模板库 / 摄食模板库 / 条件模板库（预设，可扩展）          │
│ 鱼种档案 FishProfile（宏展开 → 平铺 ResolvedProfile）      │
│ 校验（Compile 时全部静态检查）                             │
└──────────────┬────────────────────────────────────────┘
               ↓  CompiledProfile（版本化、确定性展开）
┌─ 烘焙层（离线，按 Scene × Week × 35 切片）─────────────────┐
│ 固定计算内核：Supply → Scope/份额 → P(逐Target) → A        │
│ 容量加权归一化 → EnvironmentWeight + A + 份额 + Trace      │
│ 守恒校验 / 异常兜底 / 新旧影子对比                          │
└──────────────┬────────────────────────────────────────┘
               ↓  BakeArtifact（版本化二进制/表）
┌─ 运行层（服务端权威）────────────────────────────────────┐
│ 抛投判定：位置→Target 解析 → 读 EnvWeight/A                 │
│ × ActivityMod × Bait/Presentation/Distance Match          │
│ → SelectionWeight → 旧圆桌(+NONE)                          │
└──────────────┬────────────────────────────────────────┘
               ↓
  鱼情 APP（粗粒度投影）/ 验证工具 / 遥测
```

核心对象不变：**鱼种 = 基础属性 + 习性模式集合；模式 = 空间模板 × 摄食模板 × 条件模板**。条件模板的输出从原框架的 (P, A) 两通道扩展为 **四通道**（见 D2）。

---

## 三、十二项生产决策（含对原框架的推翻与理由）

### D1 模式选择：胜者独占 → 模式份额混合（推翻）
**原框架**：`Vm = 优先级 × P × A`，取最大者为「当前模式」，其余模式不生效。
**具体化后暴露的三个问题**：
1. **循环依赖/语义歧义**：P 是逐位置的量。Vm 若用逐位置 P，则同一鱼在不同位置会处于不同模式（每个点各自选最优）；若用聚合 P，聚合方式未定义。位置权重的归一化基准因此不唯一。
2. **无法表达并存群体**：策划原始需求（产卵季 60% 普通 / 25% 产卵 / 15% 护巢，各自独立候选）在胜者独占下无解——产卵季要么全群切换要么不切。
3. **阈值抖动**：按切片独立取最大值，在环境边界（晨昏、压力阈值附近）会来回翻转，烘焙结果出现锯齿。

**修正**：模式竞争改为**份额混合**。
```text
Weight_m  = Priority_m × Scope_m × Mixability_m        （鱼种×时间片级别，不读逐位置 P）
Share_m   = Weight_m^τ / Σ_m Weight_m^τ                （F1 幂归一：零保持零；τ→∞ 退化胜者独占）
PositionRaw_t = Capacity_t × Σ_m Share_m × P_m,t        （逐位置，多模式叠加）
```
- 避险模式：Scope = 生存压力 × 避难可得性，压力趋近 1 时份额自然趋近 1（配合生存 Gate 硬清零，实现"近硬"行为）；无压力时 Scope=0 → 份额恰为 0（幂归一保证，softmax 做不到这一点）；
- 生命周期模式：允许 `FORCE_SHARE` 行直接钉份额下限（产卵季 25%）；
- SCOPE 全零兜底：排除零权重模式后归一；仍全零 → MAIN=1 并打 `fallback=SCOPE_ALL_ZERO`；
- 幂归一对 weight 量纲敏感：priority 建议域 **0.5–2.0**，进编译期校验（F1 配套）；
- **原 P2 的 FishGroup 轴被份额机制吸收**：行为学并存群体 = 带基线份额的副模式/生命周期模式。FishGroup 作为独立配置轴取消（需与策划确认，见 05 待确认清单）。

**代价**：咬口属性取该位置主导模式（`dominant_t = argmax Share_m × P_m,t`）的摄食模板，份额仅作用于分布与 A 的加权——可接受，且换来抖动消除与并存群体两项能力。

### D2 条件模板输出：(P, A) → 四通道 SUPPLY / SCOPE / PFIT / AFIT（修正）
**原框架**：条件只输出 P（空间适宜度）与 A（摄食激活度）。
**具体化后暴露的问题**：模式价值、鱼种总量、空间分布、摄食状态四件事共用了两个通道——总量（今天这种鱼整体多少）没有落点，被迫挤进 A（A 低=不想吃≈鱼少），导致"鱼少但开口"与"鱼多但停口"不可分别调参；模式价值又复用 P 造成 D1 的循环。
**修正**：每个条件行显式声明目标通道：
| 通道 | 粒度 | 回答 | 消费方 |
|---|---|---|---|
| `SUPPLY` | 鱼种×切片 | 今天这种鱼总量多少 | 圆桌前的总量缩放 |
| `SCOPE` | 模式×切片 | 这个模式今天成不成立（饵鱼群在不在、季节窗口、产卵期） | 模式份额 |
| `PFIT` | 模式×Target | 这个位置对这个模式多合适 | 空间分布 |
| `AFIT` | 模式×Target | 到了这里之后多想吃 | 咬口 |
同一事实可进多通道（分别登记、各自限幅），继承通道纪律。

### D3 P 聚合：加权算术平均 → 三层聚合（推翻原压缩，恢复早期框架）
**原压缩模型**：`P = Gate × Σ(wᵢSᵢ)/Σwᵢ`。
**问题**：算术平均是 OR 语义——伏击鱼"必须有结构"（S_structure=0）会被其他高分稀释成 P=0.6，语义错误；且权重对结果形状的控制在平均语义下很弱。
**修正**（原对话阶段 9 已论证、压缩时被丢掉，生产级恢复）：
```text
P_m,t = Gate_m,t
      × exp( Σ wᵢ·ln(sᵢ) / Σ wᵢ )        ← 核心要素：加权几何平均（AND 语义，一项为 0 则 P=0）
      × clamp( 1 + Σ cⱼ , 0.7 , 1.3 )     ← 舒适要素：加性、强限幅
```
- 核心/舒适由空间模板按要素声明（结构、水层、流态 = 核心；底质、开阔度 = 舒适）；
- 新增要素进舒适层时对存量鱼影响 ≤ ±30%，保证扩展不破坏调平。

### D4 模板继承 → 模板即宏（推翻）
**原框架**：核心原型模板 → 家族模板 → 鱼种 三级运行时继承 + 修正包混入。
**具体化后暴露的问题**：修正包（夜行/冷水/洄游）是横切关注点，会同时触碰 SCOPE/PFIT/AFIT 多个字段；三级继承 + 横修正包 = 菱形依赖，覆盖语义（deep-merge 顺序、字段级 last-wins）没有无歧义的定义，编辑器必须实现并展示一套合并代数——复杂度回到"配置 DSL"。
**修正**：**模板只在 authoring 时复制展开**。新建鱼种 = 选家族宏 → 一次性拷贝全部参数 → 策划改差异字段。ResolvedProfile 是纯平铺数据，每个字段带溯源标记（`copiedFrom: familyMacro:v3, modified: true`）。运行时零继承语义；模板升级时由 diff 工具提示"重应用或保持"。三级词汇（原型/家族/鱼种）保留为**命名空间与预设组织方式**，不再是运行时结构。

### D5 四档状态：计算量 → 展示投影（修正）
**原框架**：A 经阈值（0.25/0.45/0.75）离散为四态，四态参与位置修正与咬口。
**问题**：阈值离散使烘焙输出对参数微调不连续（策划调 0.01 出现整档跳变）；四态双语义词（既是计算量又是汇报词汇）。
**修正**：**计算全程使用连续 A ∈ [0,1]**；四态（抑制/机会摄食/正常摄食/积极摄食）只作为 UI/APP/设计沟通的投影，阈值按家族预设可配。继承原对话内部结论"保留连续 FeedingReadinessScore 再投影外部大状态"，并把它升格为硬规则。

### D6 删除 StatePositionModifier（推翻）
**原框架**：`RawWeight = BaseWeight × P × StatePositionModifier`（积极状态扩大活动范围）。
**问题**：A 既进位置又进咬口（ActivityMod），同一状态双重生效，调参纠缠。
**修正**：v1 删除该项。"积极→扩大活动范围/离结构更远"由条件行直接调 PFIT 表达（弱光→浅水草边 P+0.15），语义更显式且自动进入通道纪律。v2 若模拟显示表达力不足，再以独立声明字段恢复。

### D7 恢复 QualityShare 体型轴（补回）
**原压缩模型**丢掉了 FishQuality。生产上这是必需轴：候选单位 = 鱼种×品质（圆桌、Generate Fish、经济系统都消费）。
**修正**：FishProfile 内置 `qualities[]`：份额、重量/长度区间、个体生成参数；可选 **轻量 P 修正**（大个体偏深水/偏沉木 = 对指定空间要素的 cⱼ 加成）。完整"每品质独立模式集"不做（那是 FishGroup 的路，已被 D1 份额机制覆盖大半）。

### D8 恢复 Supply 总量通道（补回，D2 的必然推论）
`Supply_s = clamp(BasePop_s × Π SUPPLY行, MinSupply, MaxSupply)`，作用于圆桌前总量。季节强弱、时段总体活跃、运营事件走此通道。**归一化只管分布、Supply 只管总量**——"鱼从 A 挪到 B"与"全场变多"彻底解耦。

### D9 咬口通道纪律（固化为不变量）
```text
SelectionWeight_{s,q,t} = EnvWeight_{s,q,t}          ← 烘焙：分布+总量
                        × ActivityMod( A_{s,t} )      ← 烘焙：连续摄食激活
                        × AnglerMatch                 ← 运行时：三 Match 几何合成（F3）
                          = BaitMatch^λ1 × PresentationMatch^λ2 × DistanceMatch^λ3
                            （饵型/尺寸/颜色/气味 × 速度/轨迹/停顿 × 感知/攻击距离几何）
```
不变量：
- A 的输入**不含任何玩家因素**（玩家因素只进三个 Match）；
- Match 项不读鱼情状态（环境对 Match 的影响只通过 DistanceMatch 的传播折算 `EffectiveRange = BaseRange × Transmission_t`，继承阶段 8 的 EffectiveSignal 因果链）；
- EnvWeight 不读 A，A 不读 Match；四通道任何一事实只登记一次。

### D10 时序：滞回状态机 → 事实侧 EMA 平滑（修正；F4 决策 2026-09-10）
**原框架**：主习性惯性 + 切换阈值（enter/exit 双阈值）+ 最小驻留——本质是跨切片状态机，使烘焙不再是按切片独立可并行，且引入新的一类调参（阈值对）。
**修正**：平滑完全放在**环境事实侧**——环境管线对 SCOPE/PFIT 的输入（温度蓄热、钓压衰减、晨昏窗口升降、压力基线）做 EMA / 迟滞，**内核完全无状态**：切片独立、可并行、可局部重烘焙、单切片可独立回放。
**被否决的备选**（份额侧 EMA，内核持 prevShares）：平滑对象直接是份额、手感略更灵敏，但任何配置改动须整周重烘、周边界初值造成首日偏差、回放需从周首重放——三项运维代价大于手感差异，手感可在管线平滑参数中找补。
离散跳变仅保留两类**硬**行为：生存 Gate 清零（不可平滑）与 FORCE_SHARE 钉扎。

### D11 异常与兜底（继承并工程化）
完整异常表见 [04](04-计算内核与烘焙管线.md) §4。原则：配置错误编译期报错；运行期缺数据按显式 MissingDataPolicy；分布全零回退 BaseHabitatDistribution 并打警告标记（进版数据可见）；NaN/Inf 一律 Bake 失败。

### D12 版本、一致性与防作弊
- 版本四元组：`ConfigRevision / RuleRevision / EnvironmentRevision / BakeRevision`，进版与运行时校验，禁止混版；
- 服务端权威：圆桌与 SelectionWeight 在服务端；客户端只拿 **APPHint 投影**（按区域×时段的四态、推荐饵/手法标签，无原始权重）；
- 回放：`(BakeRevision, 输入, Seed)` 完整复现一次判定；
- 遥测：每次判定的通道分解入仓，供验证工具与调参。

---

## 四、最终计算公式（生产形态汇总）

### Reviewer R1 阻断项（待关闭）

- Scope 全零必须使用显式兜底模式；每个鱼种编译期强制声明且仅声明一个 fallback。
- PositionRaw 全零与数据缺失 fallback 分离；真实无适宜位置不得伪造分布。
- priority、mixability、τ 和 QualityShare 的非负、范围及归一性列为编译期不变量。
- 区分 `A_selection`（存在度加权）与 `A_dominant`（主导模式展示/攻击属性）。
- EMA 初始状态写入 EnvironmentRevision；环境事实图编译期拓扑排序，禁止循环。
- 回退开关、触发阈值、冷却和 BakeRevision 关系必须显式化。

```text
【烘焙 · 每场景×每周×35切片，按时间顺序】

facts        = EnvPipeline(slice)                     # 含派生与平滑（F4：平滑在此侧完成）
Supply_s     = clamp( BasePop_s × Π SUPPLY行 , Min, Max )
Weight_m     = Priority_m × Scope_m × Mixability_m    # Scope_m = Π SCOPE行
Share_m      = Weight_m^τ / Σ_m Weight_m^τ            # F1 幂归一；FORCE_SHARE/GATE 钉扎后重归一；
                                                       # SCOPE 全零 → MAIN=1 兜底+fallback 标记
P_m,t        = Gate_m,t × GeoMean_w(core_s) × clamp(1+Σcomfort_c, 0.7, 1.3)
A_m,t        = min( clamp01(A_base_m + Σ AFIT_delta), Π AFIT_cap )   # F2 veto 包络
PositionRaw_t= Capacity_t × Σ_m Share_m × P_m,t
EnvWeight_{s,q,t} = Supply_s × QualityShare_q × PositionRaw_t / Σ_t PositionRaw_t
A_{s,t}      = Σ_m Share_m × A_m,t                    # 份额加权（该位置种群平均意愿）
Dominant_{s,t} = argmax_m ( Share_m × P_m,t )         # F5：取（已平滑输入下的）份额 argmax
不变量: Σ_t EnvWeight_{s,q,t} = Supply_s × QualityShare_q   （守恒校验）

【运行时 · 每次抛投判定】

(t, layer)   = ResolveTarget( 饵位置, 水层带 )          # v1 最近带
AnglerMatch  = BaitMatch^λ1 × PresentationMatch^λ2 × DistanceMatch^λ3   # F3，Σλ=1，默认各 1/3
SelectionWeight_{s,q} = EnvWeight_{s,q,t} × ActivityMod(A_{s,t}) × AnglerMatch
→ 与 NONE 一起进现有圆桌
```

---

## 五、通道纪律总表（防重复计算的登记规则）

| 事实 | 允许通道（各自登记、各自限幅） | 禁止 |
|---|---|---|
| 水温 | GATE(生存边界) / SUPPLY(季节活动) / PFIT(舒适区 fit) / AFIT(摄食适温) | 再加"温度总系数" |
| 溶氧 | GATE(生存底线) / PFIT(寻氧 relief) / AFIT(缺氧抑制) | 同上 |
| 光照 | PFIT(遮蔽/寻阴 relief) / AFIT(弱光激活) / 运行时 Transmission | 直乘权重 |
| 食物密度 | PFIT(食物迁移) / AFIT(食物机会) | — |
| 饵鱼群/羽化 | SCOPE(副模式触发) / PFIT(跟随分布) | — |
| 钓压/惊扰 | SUPPLY 或 SCOPE / AFIT（事实由外部状态系统供给，烘焙只读） | 烘焙内自建历史状态 |
| 天气类型 | **只作为派生事实的因**（→光照/水温/溶氧/浑浊），不直接进任何通道 | LIGHT_RAIN × 1.10 |
| 水位/流速 | GATE / PFIT(避流 relief) / AFIT | — |
| 生命周期 | SCOPE / FORCE_SHARE / AFIT 封顶 | — |

新增要素的准入流程：声明 Owner（鱼侧/环境侧/空间侧）→ 选通道 → 登记输入与限幅 → 冲突检查（同事实同通道已有登记则拒绝编译）→ 补 Trace 模板。继承阶段 7"六件事登记"，落到编译期静态检查。

---

## 六、与既有系统的接口

| 系统 | 契约 |
|---|---|
| 旧圆桌/NONE | 输入从旧 EnvironmentWeight 换为烘焙 EnvWeight（同量纲）；NONE 标定不变 |
| 旧饵/手法适配 | P0 阶段保留旧逻辑（P0 只换 EnvWeight）；P1 阶段由 ActivityMod × 三 Match 替代，按鱼种灰度 |
| Generate Fish | 鱼种×品质 + Dominant 模式 + 摄食模板标签 + 重量长度区间（不变量：不重算任何鱼情） |
| Fish AI | 只读摄食模板的攻击投入参数（接近距离/追击距离/复攻），行为自决 |
| 保底/运营 | 外部系统，读遥测与 APPHint，通过 ControlModifier 输入口在圆桌层介入，不进烘焙 |
| 鱼情 APP | APPHint 投影（区域×时段四态 + 推荐标签），由烘焙产物派生，服务端签发 |

## 七、性能预算（量级）

- 烘焙：500 Target × 40 鱼种 × ≤4 模式 × 35 切片 ≈ 280 万条件批次/场景周，内核纯数值可向量化，单机分钟级；
- 产物：40 鱼种 × 3 品质 × 500 Target × 35 切片 ≈ 210 万条 ≈ 100–200MB（列式压缩后 10–20MB/场景周）；
- 运行时：单次判定 O(候选 × 条件行)（数百次浮点），微秒级；
- 客户端增量：仅 APPHint 表，KB 级。

## 八、风险与对应

| 风险 | 对应 |
|---|---|
| 份额混合手感不可信 | 05 文档 A/B 模拟验收（份额平滑度、热点迁移轨迹），不合格回退胜者独占+滞回（保留两套内核开关） |
| 策划不接受 FishGroup 被吸收 | D1 作为待确认项前置暴露；若否决，恢复 P2 FishGroup 轴，份额机制降级为纯行为切换 |
| 环境管线产不出逐位置事实 | 05 的管线 V0（分层插值）为 M1 里程碑前置依赖，阻塞即升级 |
| 几何平均调参不直观 | 编辑器提供"单要素灵敏度曲线"预览（固定其余要素扫一个 sᵢ）；权重语义 = 指数弹性，工具翻译成人话 |
| F4 事实侧平滑手感偏钝 | 管线平滑参数（EMA α/迟滞窗）找补；被否决的份额侧 EMA 方案已存档于 D10，必要时可切换 |
