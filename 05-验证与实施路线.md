# 05 · 验证与实施路线

> 前置：[02 架构](02-Production架构设计.md) / [03 契约](03-数据契约与Schema.md) / [04 内核](04-计算内核与烘焙管线.md)。本文回答"怎么证明它对、怎么落地"。
> 更新记录 R1.1（2026-09-10）：并入 06 复核的模拟器断言 A1–A6、F5 的 dominant 翻转率指标与 F3 的 match_compression 监控；F4 已决策（事实侧平滑），不再是待决策项。

---

## 一、模拟器（验证中枢）

**形态**：CLI + 报表，输入 = CompiledProfile + 场景 + 环境剧本，输出 = 逐切片/逐位置/逐鱼的完整通道分解 + 蒙特卡洛中鱼率。不依赖游戏客户端即可全量回归。

**能力**：
1. 参数扫描：单参数（阈值/权重/α/τ）扫描出曲线，验证 D5 连续性与 D10 平滑性；
2. 蒙特卡洛：虚拟钓手行为模型（对的位置×对的方法 / 对的位置×错的方法 / 错的位置…四象限）× N 次抛投 → 中鱼率矩阵；
3. 新旧对比：影子烘焙 diff（按鱼种/时段/位置偏差分布 + 分位数）；
4. 回放：任意 traceId 重算 bit 级一致校验；
5. 极限剧本：全场热应激、产卵季、钓压满格、溶氧崩溃——验 D1 份额与 AVOID 的行为可信度。

## 二、验收指标（初始值待校准）

| 维度 | 指标 | 初始门槛 |
|---|---|---|
| 守恒 | Σ envWeight 偏差 | < 1e-4（硬性） |
| 平滑 | 相邻切片份额 L1 变化 | P95 < 0.15（无锯齿） |
| dominant 稳定 | dominant 模式翻转率（F5） | < 2%/切片 |
| 匹配压缩 | E[AnglerMatch]（F3） | 按鱼种×时段建立基线带，异常低报警 |
| 差异 | 鱼种间同位置同时段权重比 | 最大/最小 ≥ 5× |
| 迁移 | 热应激剧本热点位移 | 主要 Target 份额转移 ≥ 40%，总量变化 ≤ Supply 限幅 |
| 方法敏感 | 对法 vs 错法中鱼率比 | ≥ 3×（P1 期指标） |
| 窗口 | 晨昏峰谷对比 | 峰/谷 A 比 ≥ 2×，且曲线连续无档位跳变 |
| 可解释 | 每条 record 命中原因 | ≥ 1 条有效 trace，fallback 率 < 0.5% |
| 回放 | 抽样 trace 重算一致率 | 100% |

## 三、8 鱼验证矩阵（验证"模板+L1 覆盖 95%"假设）

取原对话指定的 8 类代表鱼 × 10 环境情景，逐格填写业务规则到条件行/Matcher，统计 L0（纯模板）/ L1（条件行可表达）/ 超限（需框架扩展）：

| 鱼 | 主模式 | 副模式 | 避险 | 预计难点 |
|---|---|---|---|---|
| 严格底栖鱼（鲶） | 底层翻找 | 夜间近岸搜索 | 深潭富氧 | 层带 Gate + 夜行 SCOPE |
| 溪流鳟鱼 | 流缝截食 | 羽化表层 | 深潭/石后 | RELIEF 流速压力 |
| 普通鲈鱼 | 浅水结构伏击 | 开放追逐/护巢 | 深水阴影 | 三模式并存（D1 主证例） |
| 表层巡游鱼（翘嘴） | 表层追随 | 晨昏炸水窗口 | 深中层 | SCOPE 窗口 |
| 深水冷水鱼（湖鳟） | 深层温跃层 | 清晨浅层窗口 | 更深层/入水口 | 温跃层事实管线 |
| 夜行伏击鱼（黑鱼） | 草区伏击 | 白天深草休眠 | 无（耐受广） | 昼夜 SCOPE 反转 |
| 缺氧上浮鱼（鲤） | 底栖翻找 | 缺氧上浮（动态） | — | AVOID vs 动态 PFIT 的边界 |
| 季节洄游鱼（鲑） | 溯河洄游 | 产卵攻击窗口 | 高温止步 | LIFECYCLE FORCE_SHARE |

10 情景：正常 / 高温 / 低温 / 缺氧 / 强光 / 强流 / 降雨 / 食物爆发 / 不同季节 / 不同时段。
**产出**：超限格 = 框架扩展需求清单（新 Matcher 或新通道提案），而不是放开口子写规则。

## 三b、模拟器断言集（06 复核 A1–A6，直接写入回归）

| # | 断言 | 对应 |
|---|---|---|
| A1 | 无压力场景：AVOID 份额 == 0（±1e-6） | F1 幂归一 |
| A2 | 严重缺氧剧本：无论叠加多少正窗口行，A ≤ veto cap | F2 veto 包络 |
| A3 | τ 扫描：τ→∞ 时与 winnerTakeAll 路径结果一致（回退开关的正确性证明） | D1 / §五 |
| A4 | 匹配压缩：固定鱼情下，错法四象限的 NONE 占比不随 λ 显著漂移 | F3 |
| A5 | 守恒：任意剧本 Σ envWeight == Supply × QualityShare（含 FORCE_SHARE 钉扎后） | 03 不变量 |
| A6 | 热应激迁移：主要 Target 份额转移 ≥ 40% 且 Supply 变化 ≤ 限幅 | §二 回归 |

## 四、里程碑

| 阶段 | 内容 | 出口条件 |
|---|---|---|
| **M0 契约冻结** | 03/04 文档评审通过；Schema 定稿；通道纪律表冻结 | 策划+研发签字；待确认清单 §六 全部闭环 |
| **M1 内核+模拟器** | 烘焙内核、环境管线 V0（分层插值）、模拟器 CLI、golden fixtures | 8 鱼矩阵全格可跑；守恒/回放 100%；指标初测 |
| **M2 模板库+编辑器 MVP** | 空间/摄食模板各 8–12 个；鱼种编辑器（宏复制、条件行、灵敏度预览、校验）；diff 工具 | 策划独立产出 3 条新鱼不需研发介入 |
| **M3 影子烘焙+灰度 P0** | 全场景影子运行；新旧 EnvironmentWeight diff 达标后按鱼种灰度切旧圆桌输入 | 偏差分布进入设计预期带；线上遥测 fallback<0.5% |
| **M4 运行时 Response 期（P1）** | ActivityMod×三 Match 替换旧饵/手法适配（按鱼种灰度）；APPHint 上线 | 方法敏感 ≥3×；APP 点击/留存正向 |
| **M5 生态完善** | 生命周期/避险剧本调优；更多模板；遥测看板；AI 辅助配置生成 | 200 鱼种生产吞吐验证 |

关键依赖链：环境管线 V0 与关卡 Target 打标是 M1 的前置；**不阻塞**的并行项：模板库内容、编辑器 UI。

## 五、迁移与回退

- 每鱼种独立开关：`oldWeight | newWeight`，圆桌输入运行时切换，遥测双记；
- 任何指标劣化 → 单鱼种回退，不影响他人；
- 内核保留 `winnerTakeAll` 开关（胜者独占+滞回的兼容实现），D1 份额模拟不达标时整体回退不重写。

## 六、待确认清单（进入 M0 前必须闭环）

| # | 事项 | 决策方 |
|---|---|---|
| 1 | **D1：模式份额吸收 FishGroup**——策划原文的 FishGroup/Group Routing 轴是否同意取消 | 策划 |
| 2 | 原对话三对齐项：DSL 只出 Fit（不出最终权重）/ 条件表收紧自由度 / 归一化+容量加权强制 | 策划+研发 |
| 3 | 场景鱼量守恒 vs 环境变好总量可增（Supply 通道上下限语义） | 设计 |
| 4 | 保底/ControlModifier 的接口边界（圆桌层介入点） | 设计 |
| 5 | Target 打标与 capacity 数据的生产管线归属（关卡工具 vs 程序生成） | 关卡+研发 |
| 6 | 环境管线 V0 的责任团队与更新频率（35 切片/周是否最终节奏） | 研发 |
| 7 | 玩家呈现信号的采集口径（速度/停顿/轨迹的定义与精度） | 客户端 |
| 8 | 原对话 HTML 交付物中"十项待确认决策"原件找回核对 | 项目 |
| 9 | τ（份额锐度）与 λ（Match 指数）初值、管线侧平滑参数（F4）、四态投影阈值家族表 | 策划（M1 模拟辅助） |
| 10 | 200 鱼种的内容生产吞吐预期与 AI 辅助边界 | 策划 |

## 七、风险登记（动态维护）

## 八、回退决策执行规约（R1）

- 触发：同一鱼种×场景在连续 3 个观测窗口满足任一条件：fallback 率 > 0.5%、守恒失败、回放不一致、关键中鱼率相对基线下降 > 20%。
- 粒度：只回退该鱼种×场景，不影响其它鱼种；切换写入 `RollbackRevision`。
- 冷却：回退后至少 24 小时或 6 个环境切片不得自动重新启用；重新启用需通过同一 Golden Case 集合。
- 版本：保留原 `BakeRevision`，新产物生成独立 revision；回放记录 active engine、BakeRevision、RollbackRevision。
- 操作：旧内核作为只读兼容路径并行保留，所有切换和自动回填写入审计日志。

| 风险 | 概率 | 影响 | 缓解 | 状态 |
|---|---|---|---|---|
| 份额混合手感不可信 | 中 | 高 | 模拟 A/B + winnerTakeAll 回退开关 | M1 验证 |
| 策划否决 FishGroup 吸收 | 中 | 高 | 恢复独立 FishGroup 轴，份额降级为行为切换 | 待确认 #1 |
| 几何平均调参门槛 | 中 | 中 | 灵敏度预览工具 + 权重人话翻译 | M2 |
| 环境管线延期 | 高 | 高 | V0 分层插值先行的明确前置声明；里程碑阻塞升级 | 跟踪 |
| 通道纪律被绕过（私加系数） | 中 | 高 | 编译期拒绝 + 代码评审清单 | 制度化 |
