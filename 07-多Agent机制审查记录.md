# 07 · 多 Agent 机制审查记录

状态：主 Agent 综合中；并行 agent 首轮因模型容量不足未形成有效意见。

## 共同问题底稿

目标：把固定鱼习性模型拆成语义正确的机制，再推导中层路径、规格和原型。当前 Supply/Scope/P/A、Mode、FishGroup、Quality、模板宏、烘焙/运行时边界均视为候选，不视为已冻结事实。

原始业务现象：鱼会因环境、资源、周期和玩家呈现发生出现、迁移、停留、进食概率变化；系统必须能表达总量变化、空间迁移、摄食意愿和玩家匹配之间的差异，并可解释、回放和验证。

## 首轮审查框架

### A 现实机制

需区分：环境事实（温度、溶氧、流速、光照、资源、钓压）与鱼群状态（生存压力、能量需求、空间选择、摄食意愿、群体组成）。出现/停留/迁移/进食不是同一事件；硬 Gate 只用于不可生存或不可达条件，其他变化优先保持连续。

### B 因果语义

必须逐项证明：Supply 只改变鱼种总量；Scope 只改变模式成立或份额；P 只改变空间分布；A 只改变摄食激活。若同一事实同时通过多条路径影响同一输出，必须说明是独立因果还是重复计权。Mode 与 FishGroup 不能同时承担群体切分和行为选择而没有边界。

### C 反例攻击

最低覆盖：鱼少但想吃、鱼多但不开口、环境差但资源强、多个模式并存、模式边界抖动、质量比例变化、玩家方法影响咬口但不改变鱼群分布、缺失数据和全零分布。

### D 生产可行性

检查策划是否能维护字段，模板是否产生隐式继承，慢变量是否可烘焙，快变量是否应运行时计算，结果是否可版本化、回放、灰度和回退。

## 当前主 Agent 初步综合

1. 四通道是有价值的候选，但必须先证明四者对应不同现实事件；不能因为公式方便就保留。
2. P/A 分离是必要方向，但 A 不应被解释为最终咬口概率；玩家匹配必须保持独立输入。
3. Supply 与 QualityShare 需要区分“鱼群总量”和“抽取/品质组成”；若质量只是生成权重，则不应伪装成生态状态。
4. Mode 若只是配置组织概念，可在编译后消失；若它代表同时存在的真实群体，则必须保留实体边界，不能仅改名为份额。
5. 模板宏展开能消除运行时继承复杂度，但会增加复制后的漂移与升级成本；必须配套溯源、差异检测和重应用策略。
6. 烘焙与运行时的切分必须按因果时间尺度决定，不能只按性能决定。

## 尚未冻结的关键问题

- Supply 是否代表生态总量、可用鱼量或抽取预算。
- P 是否包含容量，还是容量仅属于 Target。
- A 是否是生理摄食意愿、资源响应，还是接近咬口的中间量。
- 模式份额是否代表真实并存群体。
- QualityShare 的生态语义与抽取语义。
- 缺失事实是未知、默认值还是禁止计算。

## 设计闸门

在上述问题通过三类场景和反例推演前，不冻结公式、Schema 或代码实现；未解决冲突保留为异议，不提升为主线结论。

## 首轮有效审查意见（因果语义 Agent）

- A 必须按模式存在度 P 加权；否则鱼已迁离 Target，原模式仍贡献摄食意愿。
- dominant、A 与 feeding/match 模板必须统一语义：要么全部按存在度混合，要么全部取 dominant，不能混用。
- QualityShare 的生态组成语义与抽取配额语义必须拆开；质量行为修正不能悄悄改变品质组成。
- 同一事实进入多个通道时必须有主路径和增益预算，单纯登记并不能消除重复计权。
- 生存 Gate 不应自动推导 AFIT veto；两者需由鱼种生理配置分别声明。
- FishGroup 只有在纯 routing 标签时才能被模式份额替代；真实并存群体必须保留群体级边界。
- Scope 全零不能无条件 MAIN=1；必须区分基线仍出现和窗口失效即不可见。
- 真实 P 全零与数据缺失 fallback 必须分开处理。
- Transmission、Target 解析规则和 tie-break 必须纳入版本与回放输入。
- 模板 provenance 需要字段级 base hash 和三方 diff，避免宏复制后的静默漂移。
- 环境事实平滑与鱼侧模式窗口平滑不能默认共用同一 EMA，且要防止 fishingPressure 双重平滑。

## 已采取的候选修正

计算内核的 A 汇总已改为按 `Share × P` 归一的存在度加权；P 为零时 A 明确为零。该修正仍处于候选状态，需经场景推演和反例验证后再冻结。

## 顶层 Reviewer R1（持续上下文）

Reviewer 指出 8 项阻断：显式 fallback 模式、PositionRaw 全零策略、份额参数边界、A_selection/A_dominant 分叉、QualityShare 归一性、EMA 初始状态回放、事实图 DAG、旧内核回退控制。已把这些问题加入 02 顶层架构文档的待关闭清单；在全部通过前不标记顶层冻结。
