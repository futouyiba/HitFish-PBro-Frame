# 04 · 计算内核与烘焙管线

> 本文实现 [02](02-Production架构设计.md) 的公式与 [03](03-数据契约与Schema.md) 的契约：内核伪代码、遍历与平滑、异常兜底、确定性与版本、运行时流程。
> 更新记录 R1.1（2026-09-10）：并入 06 复核 F1（softmax→幂归一）/ F2（veto 包络）/ F3（AnglerMatch）/ F4（平滑移至事实侧，内核无状态）——本文公式为并入后版本。

---

## 一、内核伪代码（烘焙主循环）

```pseudo
function BakeScene(scene, week, compiled[], envPipeline, targets[]):
    # 编译期已保证：条件行按 (channel, priority) 静态排序；常量折叠完成
    # F4：内核完全无状态——一切平滑在环境管线侧完成，切片可乱序/并行/局部重烘

    for slice in week.slices:                                   # 顺序仅为产物组织，非依赖
        facts = envPipeline(slice)                              # 含派生事实与平滑
        out = []

        for profile in compiled:                                # 鱼种
            # ---- 1. 生存场景级 Gate（先于一切，避免无效遍历）----
            if any(survivalGate(profile.base, facts.scene) fails with ERROR policy):
                emitAll(profile, weight=0, reason=SCENE_GATE); continue

            # ---- 2. Supply（总量通道，鱼种×切片）----
            supply = clamp(profile.basePop × Π applyRows(SUPPLY, facts),
                           profile.supplyClamp)

            # ---- 3. Scope 与份额（模式×切片；不读逐位置数据）----
            for m in profile.modes:
                scope[m] = clamp01(Π applyRows(SCOPE, m, facts))
                weight[m] = m.priority × scope[m] × m.mixability
            shares = powerNormalize(weight, τ=2.0)              # F1 幂归一：零保持零
            if allZero(shares):                                 # SCOPE 全零兜底
                shares = {MAIN: 1.0}; flag fallback=SCOPE_ALL_ZERO
            for m with FORCE_SHARE rows: shares = pinFloor(shares, m, force)  # 钉扎后重归一
            if allNonAvoidGatesFailed(profile, facts): shares = {AVOID: 1.0}  # 硬兜底

            # ---- 4. 逐模式 × 逐 Target 的 P 与 A ----
            for t in targets:
                for m in profile.modes:
                    g = gates(m.spaceTpl, t, facts)             # 任一失败 → P=0（唯一短路）
                    core = exp( Σ wᵢ·ln(match(sᵢ,t)) / Σ wᵢ )  # D3 几何核心
                    comfort = clamp(1 + Σ applyRows(PFIT, m, t, facts), 0.7, 1.3)
                    P[m,t] = g × core × comfort
                    A[m,t] = min( clamp01(m.aBase + Σ AFIT_delta行),
                                  Π AFIT_cap行 )                # F2 veto 包络（一票否决）

            # ---- 5. 分布与守恒（品质轴）----
            for q in profile.qualities:
                for t in targets:
                    pOver = Σ_d(pOverrides(q, channel=PFIT))    # D7 轻量体型修正
                    raw[t] = t.capacity × ( Σ_m shares[m] × clamp01(P[m,t]×(1+pOver)) )
                Z = Σ raw
                (raw, fallbackFlag) = zeroDistributionPolicy(profile, raw)   # §四.3
                for t in targets:
                    envW = supply × q.share × raw[t] / Z
                    presence = Σ_m shares[m] × P[m,t]
                    a[t] = presence > 0
                           ? Σ_m(shares[m] × P[m,t] × A[m,t]) / presence
                           : 0
                    dom[t] = argmax_m(shares[m] × P[m,t])       # F5：输入已平滑，翻转受指标监控
                    out.push(record(slice, profile, q, t, envW, a[t], dom[t],
                                    shares, trace(slice, profile)))
        checkInvariants(out)                                    # §五 守恒/NaN/份额
    return artifact(out, revisions)
```

要点：
- **条件行求值 `applyRows` 无分支**：预排序数组顺序应用（ADD_DELTA 累加受 cap、MUL 乘、CLAMP 钳、AFIT_CAP 进 veto 连乘、GATE_FAIL 标记）。无 Early Return——Gate 是模板层的唯一短路；
- `powerNormalize(weight, τ)` 与 `pinFloor` 均为纯函数，确定性可测；
- **切片完全独立：内核无任何跨切片状态（F4）**——可乱序/并行、可局部重烘、单切片可独立回放。

---

## 二、平滑与派生事实（环境管线职责）

| 需求 | 实现 | 理由 |
|---|---|---|
| 水温蓄热/冷空气延迟 | 管线内 EMA / 双蓄水池 | 继承"迟滞在环境层"决定；内核零历史状态 |
| 钓压衰减 | 外部状态系统写 `events.fishingPressure`（带衰减） | 钓压是持久因果状态，非本系统职责 |
| 晨昏窗口 | `derived.duskWindow ∈ [0,1]` 三角窗 | 条件行读窗口值，不读钟表 |
| 压力基线 | `derived.heatStressBase` = 鱼无关的场景级压力（perTarget 由 RELIEF matcher 消化） | 两步机制：场景压力 × 位置缓解 |
| 模式窗口/份额输入平滑（F4） | 管线对 SCOPE 输入（窗口值、压力基线、事件强度）做 EMA/迟滞 | 内核无状态；手感差异在管线参数找补 |

---

## 三、运行时流程（服务端权威）

```pseudo
function EvaluateCast(session, req):
    t = resolveTarget(req.baitPos, req.layer)         # 最近带；v2 插值
    bake = loadArtifact(scene, week)                  # 校验 revision 四元组
    for (fish,q) in candidates(t):
        envW  = bake[fish,q,t].envWeight
        aMod  = pow(bake[fish,t].a, fish.activityGamma)        # D5 连续
        bMatch  = matchBait(fish.feeding(dominant).baitAffinity, req.baitDesc)
        pMatch  = matchPresentation(..., req.presentationStats)
        dMatch  = matchDistance(..., effectiveRange(t))        # × Transmission_t
        angler  = bMatch^λ1 × pMatch^λ2 × dMatch^λ3            # F3，Σλ=1，默认各 1/3
        w[fish,q] = envW × aMod × angler                       # 全 clamp01 上界
    draw = roundTable(w ∪ {NONE: noneWeight(req, session)})    # 旧圆桌不动
    telemetry.emit(traceId, w 的通道分解, revisions, req.seed)
    return draw
```

- P0 期：`aMod × bMatch × pMatch × dMatch` 整体由**旧饵/手法适配函数**替换注入（接口相同），实现"只换 EnvironmentWeight"；
- 防作弊：客户端可见仅 `hints[]`；`weightDetail` 只进遥测与服务端日志。

---

## 四、异常与兜底总表

| # | 异常 | 策略 | 层级 |
|---|---|---|---|
| 1 | 配置引用/数值/通道冲突 | 编译失败，错误定位到 rowId/字段 | 编译期 |
| 2 | 场景级生存 Gate 失败 | 该鱼种全切片 weight=0，记录原因（设计上允许） | 烘焙 |
| 3 | 某品质所有 Target 的 raw=0（有效空间存在但 Fit 全零） | 回退 BaseHabitatDistribution（=各模板 TAG_LOOKUP 基线分布）；无基线则容量均匀分布；记录 `fallback` 标记，批次 fallback 率 >0.5% → Bake 失败 | 烘焙 |
| 4 | 所有 Target 均未过 Gate | 设计允许→weight=0；场景声明"必须出现"→ Bake Validation Error | 烘焙 |
| 5 | 事实缺失 | 按 MissingDataPolicy：Gate 相关只允许 ERROR；其余 USE_DEFAULT/SKIP 显式 | 运行前 |
| 6 | NaN/Inf | 立即 Bake 失败，附带 fish×mode×target 定位 | 烘焙 |
| 7 | 守恒校验失败（Σ≠Supply×Share 超 1e-4） | Bake 失败 | 烘焙 |
| 8 | 运行时 revision 不匹配 | 拒绝判定，回退上一 BakeRevision + 告警升级 | 运行时 |
| 9 | FORCE_SHARE 钉扎冲突（Σ force > 0.6） | 编译失败 | 编译期 |
| 10 | Target 解析落空（饵在未定义水域） | 就近合法 Target + 遥测计数（监控关卡打标覆盖） | 运行时 |
| 11 | SCOPE 全零（所有模式窗口同时消失） | MAIN=1 兜底 + `fallback=SCOPE_ALL_ZERO` 标记（F1 配套） | 烘焙 |

---

## 五、确定性与版本

1. **迭代顺序固定**：一切循环按 ID 字典序；条件行按 (channel, priority, rowId) 编译期排序；
2. **浮点落盘精度固定**（1e-6），跨平台 bake 结果 bit 级一致由 CI 的 golden fixture 保证（每版本：3 场景 × 8 鱼 × 极端天气周）；
3. **版本四元组**（Config/Rule/Environment/Bake）写入每条 record；运行时加载校验，禁止混版；
4. **回放**：`(traceId → revisions + req + seed)` 可完整复现 `weightDetail`；
5. **新旧并行**：影子烘焙任务同时产出旧 EnvironmentWeight 与新 envWeight，diff 报表（按鱼种/时段/位置的偏差分布）作为灰度开关依据。

## 六、遥测事件（最小集）

| 事件 | 字段 | 消费方 |
|---|---|---|
| `cast_evaluated` | traceId, slice, target, fish, 通道分解（含 angler）, draw | 验证工具 / 调参 |
| `match_compression` | E[AnglerMatch]，按鱼种×时段 | F3 监控：异常低=三 Match 权重设计问题而非鱼情问题 |
| `bake_completed` | revisions, 记录数, fallback 率, 守恒误差, 耗时 | 流程监控 |
| `gate_failed_scene` | fish, slice, 原因 | 策划巡检 |
| `fallback_used` | fish, q, slice, 原因 | 策划巡检 |
| `hint_served` | zone, slice, state, tags | APP 效果评估 |
