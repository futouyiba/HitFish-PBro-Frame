# 完整目标复审：早期通过结论的范围修正

状态：Open / 未冻结。此记录解释为什么先前局部 reviewer 通过不能代表整体完成。

## 正确资料落位

PBroFrame 入口：https://app.notion.com/p/3d7a4137d23681cdb9a8fb6fc801f711

同分支较晚的宏观工作台：https://app.notion.com/p/3d7a4137d23681c399f6f77b00fabbbf

此前同步到 3d8a4137d2368109b059d625f5e3bfe5 的是另一个恢复分支的简稿，不能据此声称 PBroFrame 02–05 已同步。原页保留为历史证据，不静默覆盖或删除。

治理依据：Documentation Governance v23；重要设计保存 Contract 与 Rationale，验证材料独立；review 只覆盖声明的 scope，不自动扩大到整体目标。

## 重新打开的机制

先求平均摄食意愿，再乘主导模式匹配，破坏联合分布。

两模式存在度各 0.5，A=[1,0]，Match=[0,1]。

逐模式结果：0.5×1×0 + 0.5×0×1 = 0。

旧式若取第二模式匹配：E[A]×Match_dominant=0.5×1=0.5。增加 a_dominant 字段不能修复这个错误。

候选修正：保留模式直到玩家响应完成，先算每个模式的空间权重×ActivityMod(A)×Match，再汇总 Species×Quality。非线性 ActivityMod 也必须在模式内计算。该方向与同分支工作台一致，但不能据此省去具体规格审查。

强制 FALLBACK 只是明确了配置字段，没有证明全窗口关闭时应该出现鱼。需独立决定无候选、显式基线或失败；禁止把错误数据和真实不适宜混为一谈。

## 完成证据缺口

| 要求 | 当前证据 | 判定 |
|---|---|---|
| 顶层/中层完整语义复审 | 旧02–04局部字段一致性通过；较晚工作台未纳入 | 未完成 |
| reviewer连续上下文 | reviewer_spark、causal_retry已复用并交叉审查 | 持续中 |
| 交互原型 | 输入不驱动计算，按钮仅alert | 未完成 |
| 计算原型 | 预先提供P/A；单quality；无完整玩家响应 | 未完成 |
| 环境回放 | 只有哈希，无快照/执行重放 | 未完成 |
| HTML图形速览同步 | 手工追加；无变更绑定与视觉验收 | 未完成 |
| Notion治理与分支落位 | 真正分支已定位；简稿写在另一分支 | 待修 |
| 共享资料库 | 尚未确定原始文档入口，不以其它分支设计替代 | 待读取 |
| GitHub | 远端main已核对；本轮变更待审核提交 | 已具备 |

## 本轮独立工程修复

版本哈希改用排序JSON和SHA-256，不再使用依赖字典插入顺序的repr；CLI哈希包含实际消费的P/A/容量输入。该修复仅覆盖当前简化输入，不能宣称完整环境回放已完成。

空间分布拒绝负数、NaN、Inf、布尔值；缺失事实明确报错，真实全零仍输出零分布。避免分母夹紧而分子保留负数产生负分布。

## 继续路径

以本分支工作台和共享原始资料完成机制基线；双agent交叉审查后重写单一契约。保留旧稿为历史，避免靠追加相互矛盾的补充条款通过审核。之后实现真实编辑—计算—回放链并验证。
