# 06 · 独立复核与增补（R1）

> 复核方法：本复核由另一条独立推导链完成——**先不读 02–05**，仅以 01 档案的最终框架为输入，独立完成生产级压力测试推导；随后与 02–05 逐条对照。因此本文的价值有两种：交叉验证（两条独立链收敛的结论置信度高）与找缺口（只有一边发现的问题）。
> 结论先行：**02–05 架构成立，可进入 M0 评审**；但 F1/F2 两项属公式级必须修正（不改架构，改公式形态），F3 建议、F4 需 M0 前决策、F5 已修。

---

## 一、独立推导与 02 决策的对照（收敛度）

| # | 我的独立结论 | 02 对应 | 状态 |
|---|---|---|---|
| 1 | 胜者独占模式选择有阈值抖动、烘焙序列依赖（惯性需跨切片状态）、丢失并存群体 → 份额混合，且份额天然吸收 FishGroup 轴 | D1（+D10 EMA 替代滞回） | **收敛**（我最初倾向"平滑完全放环境事实侧"，02 选择份额 EMA——见 F4，需 M0 决策二选一） |
| 2 | 模式价值复用 per-target P 造成循环依赖 → 拆模式级成立度（我称 Scope）与逐位置 P | D2（进一步扩成 SUPPLY/SCOPE/PFIT/AFIT 四通道，比我多拆出 SUPPLY，更完整） | **收敛且被超越** |
| 3 | P 的加权算术平均丢失 AND 语义 → 恢复 Gate × 几何核心 × 限幅修正三层 | D3 | **收敛** |
| 4 | A 纯加性无法表达一票否决 → 需要"加性增量 × 乘性上限包络" | 02 未在公式层处理（AFIT 行可写 CLAMP_HI 但靠作者纪律） | **缺口 → F2** |
| 5 | StatePositionModifier 与 ActivityMod 双重生效 → 删除，积极时的空间效应走条件行 | D6 | **收敛** |
| 6 | 运行时三级继承 = 菱形依赖 + 合并代数不透明 → 模板即宏、保存期展开 + 溯源 | D4 | **收敛** |
| 7 | 压缩丢掉了体型轴与总量轴 → 恢复 QualityShare 与 Supply，且"分布迁移/总量变化"解耦 | D7 + D8 | **收敛** |
| 8 | 归一化需容量加权、全零回退显式、守恒为硬校验 | 03 §四 不变量 + 04 §四 | **收敛** |
| 9 | 数据量与烘焙预算量级估算 | 02 §七 | **收敛**（量级一致：10⁵–10⁶ 记录/场景周） |
| 10 | 确定性：迭代序固定、浮点精度固定、golden fixture、版本四元组、回放键 | 04 §五 | **收敛**（EMA 是唯一例外状态，见 F4） |
| 11 | 避险/生命周期保留覆盖语义（硬），不混入连续竞争 | FORCE_SHARE 钉扎 + AVOID 硬兜底 | **收敛** |
| 12 | L1 不是语言，是"条件行"：单事实比较 + 有限动作枚举 + 编译期排序与冲突检查 | 03 §2.3 ConditionRow | **收敛**（02 的动作枚举比我的更完整） |
| 13 | 通道登记表 + 编译期静态强制（同事实同通道单写者） | 02 §五 + 03 §六 | **收敛** |
| 14 | 运行时五因子连乘的压缩性与 NONE 标定耦合 | 02 未处理 | **缺口 → F3** |

两条独立链在所有架构级判断上零分歧——这基本排除了"顺着原框架惯性走"的风险；分歧只在公式形态与工程细节，即下文 F1–F5。

---

## 二、缺口与修正提案

### F1（必须，公式级）份额归一的零值语义：softmax → 幂归一

02 的 `Share_m = softmax(Weight_m, τ)`。softmax 的性质是 **w=0 仍有非零份额**（∝ e⁰）。取 02 自己的示例参数（τ=2，四模式：MAIN/副/生命周期/AVOID），无环境压力时 AVOID 的 Weight = 1.5 × 0 × 1.0 = 0，softmax 给它的份额约为 e⁰/(e²+e⁰+…) ≈ 8–12%。语义后果：**平静水面常态下也有约一成鱼群待在避险区**——与"避险 = 压力驱动的响应行为"的模型定义直接矛盾，且该份额还会吃掉守恒总量。

**修正**：改为幂归一（乘性零保持零）：

```text
Share_m = Weight_m^τ / Σ_m Weight_m^τ      （Weight_m ≥ 0；Weight=0 → Share=0）
```

τ 的语义不变（τ→∞ 退化为胜者独占，=0 退化为均匀），仍是一个全局手感旋钮。配套两个边界：
- SCOPE 全零（所有模式窗口同时消失，理论上不应发生）：排除零权重模式后归一；仍全零 → MAIN 兜底（=1）并打 `fallback=SCOPE_ALL_ZERO`；
- 幂归一对 weight 的量纲敏感（priority 的绝对值现在影响集中度），编译期将 priority 建议域定为 0.5–2.0 并写入校验。

### F2（必须，公式级）AFIT 缺少否决包络：加性增量 → 加性 × 乘性上限

02 的 `A = clamp01(aBase + Σ AFIT行)`。加性形态允许**正负抵消**：黄昏窗口 +0.25、食物爆发 +0.2 可以抵掉"严重缺氧 −0.4"，模型会给出"缺氧水域里黄昏照样积极开口"。原对话在压缩前的框架里本来有 BLOCK/MIN/MAX 硬规则层，压缩成 (P,A) 两通道时丢掉了；02 的 ConditionRow 虽有 CLAMP_HI 动作，但**是否写这行完全靠策划自觉**——这正是原对话反复强调的"通道纪律不能依赖纪律本身，要依赖结构"。

**修正**：公式层增加乘性上限（veto 连乘）：

```text
A_m,t = min( aBase_m + Σ AFIT_delta行 , Π AFIT_cap行 )
```

- `AFIT_cap` 行 = 新动作类型（或复用 CLAMP_HI 但声明 `veto:true` 进连乘组）；
- 模板库内置**标准 veto 包**（严重缺氧 cap 0.15 / 超出摄食适温 cap 0.2 / 极端流速 cap 0.25，数值待 05 模拟校准），宏复制时默认带入（D4 机制天然支持"默认带包"）；
- 编译期校验：凡生存 Gate 含溶氧/温度的鱼种，AFIT 必须存在对应 veto 行（结构化保证，不再靠自觉）。

### F3（建议，运行时）匹配三因子连乘 → 加权几何平均合成

02 运行时 `SelectionWeight = envW × aMod × bMatch × pMatch × dMatch`，五因子各自 ≤1。比值语义不变（好法/坏法的**相对**差异保留），但**绝对压缩**系统性发生（五因子各 0.6 时整体 0.047），而圆桌里还有 NONE 参与标定——全场景普遍压缩时 NONE 命中率上升，玩家感受为"全体鱼厌饵"，且这是调参层看不见的隐性耦合。

**修正**：三 Match 合成为单一因子：

```text
AnglerMatch = bMatch^(λ1) × pMatch^(λ2) × dMatch^(λ3)    （λ 默认 1/3 each，Σλ=1）
SelectionWeight = envW × aMod × AnglerMatch
```

指数 λ 让"压缩度"变成显式可调参数（λ 和越小、整体越接近 1，压缩越温和），NONE 标定随 λ 稳定。配套遥测指标：`match_compression = E[AnglerMatch]`（按鱼种×时段监控，异常低说明三 Match 权重设计问题而非鱼情问题）。

### F4（M0 前必须决策）EMA 的位置：份额侧 vs 事实侧

02 D10 把跨切片平滑放在**份额 EMA**（内核持 prevShares 状态）。代价：
1. 任何 Config/Rule 改动必须**整周重烘**（EMA 链依赖完整时间序列），不能局部重烘焙——影子烘焙与灰度迭代的操作成本变高；
2. 周边界初值（MAIN=1）造成首日系统性偏差（04 §四 #11 已承认）；
3. 回放需从周首重放，单切片不可独立复现。

备选：把平滑完全移到**环境事实侧**（管线对 SCOPE/PFIT 的输入做 EMA，内核保持无状态、切片完全独立可并行可局部重烘）。代价：平滑对象从"份额"变成"份额的输入"，非线性后平滑效果略不同（更钝）。

**二选一即可，但必须在 M0 冻结**——它决定内核是否有状态，属于契约级选择。我的倾向：事实侧（无状态内核的可运维性价值大于平滑手感差异，且手感差异可在管线平滑参数里找补）。

### F5（已修，文档级）README 通道数不一致 + dominant 翻转

- README"一句话概括"写**三通道**（Scope/P/A），02/03 实为**四通道**（SUPPLY/SCOPE/PFIT/AFIT）——本 R1 已同步修正 README；
- `dominant_t = argmax(Share×P)` 在两模式接近时逐切片翻转，而运行时 BaitMatch 读的是 dominant 的摄食模板 → 匹配曲线不连续。低风险（EMA/幂归一后翻转概率已降），建议 dominant 取**平滑后份额**的 argmax 并在 05 平滑指标中加"dominant 翻转率 < 2%/切片"。

---

## 三、对 05 验证矩阵的补充用例（直接可写进模拟器断言）

| # | 断言 | 对应 |
|---|---|---|
| A1 | 无压力场景：AVOID 份额 == 0（±1e-6） | F1 |
| A2 | 严重缺氧剧本：无论叠加多少正窗口行，A ≤ veto cap | F2 |
| A3 | τ 扫描：τ→∞ 时与 winnerTakeAll 路径结果一致（回退开关的正确性证明） | D1/05 §五 |
| A4 | 匹配压缩：固定鱼情下，错法四象限的 NONE 占比不随 λ 显著漂移 | F3 |
| A5 | 守恒：任意剧本 Σ envWeight == Supply × QualityShare（含 FORCE_SHARE 钉扎后） | 03 不变量 |
| A6 | 热应激迁移：主要 Target 份额转移 ≥ 40% 且 Supply 变化 ≤ 限幅（与 05 §二重复，保留为回归） | 05 |

## 四、复核结论

1. 02–05 的十二项生产决策中，与独立推导链**全部收敛**的部分（D1–D3、D4–D8、通道纪律、确定性框架）置信度高，可按 M0 流程推进；
2. F1（幂归一）与 F2（veto 包络）为公式级修正，改的是形态不是架构，工作量小，应并入 02/04 后再冻结契约；
3. F4 是唯一需要在 M0 前明确拍板的架构级选择（内核有状态与否）；
4. 本复核本身构成对"下钻不保护原结论"要求的第二重满足：不仅推翻了原对话的压缩模型（与 02 相同的七处），也修正了下钻第一稿的两处公式缺口。

---

## 五、决策记录（R1.1，2026-09-10）

| 项 | 决策 | 落点 |
|---|---|---|
| F1 幂归一 | **采纳**：`Share_m = Weight_m^τ / Σ Weight_m^τ`；SCOPE 全零→MAIN 兜底+fallback 标记；priority 域 0.5–2.0 进编译校验 | 02 D1/§四、04 §一、03 §六 |
| F2 veto 包络 | **采纳**：`A = min(clamp01(aBase + Σ delta), Π cap)`；新增 `AFIT_CAP` 动作；标准 veto 包随宏默认带入；编译期强制 | 02 §四、03 §2.3/§六、04 §一 |
| F3 AnglerMatch | **采纳**：三 Match 几何合成（Σλ=1，默认各 1/3）；遥测 `match_compression` | 02 §四、04 §三/§六、05 §二 |
| F4 EMA 位置 | **决策：事实侧**（内核无状态：可并行/局部重烘/单切片回放）；份额侧 EMA 存档否决，理由与回退路径记录于 02 D10 | 02 D10、04 §一/§二 |
| F5 文档与指标 | README 四通道已修（前会话）；dominant 翻转率 < 2%/切片 已入验收指标 | 05 §二 |

> 合并说明：本页为完整版（原两条 06——本会话全文版与并行会话精简版——已合并为此一版，Notion 端保留本页为唯一 06）。
